"""Instrument model for the MF SATs (2026 Commissioning Paper).

The importable library lives here; the runnable analyses are in
``smcim.cli`` (exposed as the ``smcim-*`` console scripts and as thin
wrappers in the repo's ``scripts/`` folder). Per-system configuration is
in the repo's ``configs/`` folder.
"""

__version__ = "0.1.0"
