"""Command-line entry points for the instrument-model analyses.

Each module here exposes ``main(argv=None)``; they are wired up as the
``smcim-*`` console scripts in ``pyproject.toml`` and mirrored by thin
wrappers in the repo's ``scripts/`` folder.
"""
